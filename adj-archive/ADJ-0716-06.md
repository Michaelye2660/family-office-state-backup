# ADJ-0716-06 · CC-0713-02 moat弹药持久化终裁落地（vendored＋venv合并方案）

- **编号**：ADJ-0716-06
- **类别与校验行**：【共5项·判断类】（全回路：执行+逐项编号回报）
- **用户已确认**：委托人「准」（2026-07-16对话内·对GM四门意见+执行清单之终裁）；授权链=〔M〕70（委托人「送GM审等意见·终裁前不执行」）→GM-4意见呈报（四判断门+执行清单）→委托人「准」。
- **投递主体**：[裁决侧·GM-4]

## 执行项（共5项）

**① vendored落库（纯文件层）**：自上游 ai-berkshire 锁定hash `e7a912a55f32`（〔M〕62锁定HEAD）取子集——19 skills（md）＋tools（含 financial_rigor.py）＋LICENSE原文——落 `vendor/ai-berkshire/`；目录内建 `VENDORED.md` 注记（上游URL＋锁定hash＋MIT LICENSE指针＋两条纪律：**勿直改上游文件；升级须走ADJ、不自动拉新**）。**edgartools（pip包）不vendored**。若本会话容器已无7/13副本，按锁定hash自上游重取，取毕核对hash申报。

**② setup脚本落库（venv隔离·拆雷正解）**：`tools/setup-moat-env.sh`——创建独立venv（如 `/home/claude/venv-edgar`）→ `pip install edgartools==5.42.0`（**锁版本**）→ 最小冒烟一行 → 明确输出成功/失败。系统环境httpx（mootdx／a-stock-data域）**零污染**。护栏：零秘密值（N8无涉）；网络仅pypi＋既放行之sec.gov域；装配失败不阻断会话其余工作、如实申报。脚本头部注释写明上述边界。

**③ moat工作指引§五装配段改写**：「每会话手动重装」→「运行 `tools/setup-moat-env.sh` 一键装配：edgartools走venv-edgar（调用时用venv内python）；ai-berkshire直接读 `vendor/ai-berkshire/`，无需安装」。工作指引版本行按**库内现行版本号顺延**（CGM按实况+0.1，回执申报新版本号，勿臆测）。

**④ 验证点设计缺陷入〔M〕（记录不豁免）**：原设「7/14晨报a-stock-data实跑」验证点**测不到雷**——简报每次全新容器、从不装edgartools，跑通≠共存成立；唯一有效读数=7/13同容器import冒烟（弱证据）。本案以venv隔离**结构性消雷**取代共存验证。缺陷系CGM设计、GM本轮复核查出——定性照写，颜色归7-19 cc-auditor照章判。

**⑤ motion闭环**：CC-0713-02.md文末追加「GM意见段（四门裁定要点）＋委托人终裁『准』（2026-07-16）」引注，状态标结；〔M〕追加一行摘要（含四门裁定：vendored限纯文件层可／venv为httpx隔离正解／供应链纪律=锁hash锁版本LICENSE＋ADJ升级门／setup护栏N8合规）＋版本历史行顺延＋结构三验照跑。

## 执行注
- 本件不动任何持仓、触发器、红线字段；纯制度与工具层落地。
- 首个moat会话实跑setup脚本＝自然验收点（venv内edgartools冒烟＋同容器a-stock-data照常），结果入该案回执。

——[裁决侧·GM-4]，2026-07-16
