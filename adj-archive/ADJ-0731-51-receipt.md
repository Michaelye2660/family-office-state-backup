# 回执 · ADJ-0731-51（共 5 项·判断类·零仓位零交易零建议）

- **执行主体**：CGM-G4（claude-opus-5）｜**执行日**：2026-07-31｜**台账**：〔M〕459·v16.396
- **四字段逐字过**（E9／`7c41d9a0…`／`50db01fa…`／`DOC_SEQ=ADJ-0731-51`）｜**三要素齐备**（③＝委托人「**修**」）｜轮 `e3d1·终`
- **结构四验（逐项贴出所核对象）**：①编号连续 458→459 ②〔N〕节标题＝1 ③件头 v16.396 ∧ 版本行 ∧ 正文三者同现 ④乱码两类全库各 0

---

## 〇 · 四处缺陷本席**逐条机械核过，四处全成立**——不辩解

**本席未采信其转述，逐条实证：**

| 指认 | 本席实证 | 判 |
|---|---|---|
| ① DOC_SEQ 双值并存 | `grep -c "DOC_SEQ 起续"` ＝ **2**（`ADJ-0731-50` 与 `ADJ-0801-01` 并列且未标何者现役） | 🔴 成立 |
| ② 龄被重置为 0 | 包内书 C3/C4/C5 ＝ **0 日**；而 `tracking-table` 第 82 行载「**2026-07-27／4 日**」 | 🔴 成立 |
| ③ 块1 误捕一行 | 该 5 列行确在 2 列表内（`sed` 抽取〔H-0〕节所致） | 🔴 成立 |
| ④ 块5 漏 SPGI | 块5 仅取〔K-0〕，SPGI 只见于块4 | 🔴 成立 |
| ⑤ 块3「当前」无时点 | 属实——**且块6 自己正写着「引用须同书读取时点」** | 🔴 成立 |

**🔴 ② 之定性本席特记，其比缺陷本身更要紧**：

> **积压龄被包重置，即掩盖真实滞留时间**……**一个把龄归零的交接载体，会让每一次交接都成为积压的洗白点。**

**本席原写「龄（自 2026-07-31 起算）」时，是把「本包生成日」当成了「事情开始的日子」**——**与本席今日所犯之「把自己的在任期当成纪元全长」（〔M〕454④）系同一形态：以自己入场的时刻，充事情的起点。**

---

## 一 · 五项修毕 · 验收判据逐条自跑

| # | 判据 | 自跑结果 |
|---|---|---|
| 1 | 块2 `DOC_SEQ 起续` **仅一行** | 🟢 `grep -c` ＝ **1**；输出 `DOC_SEQ 起续: ADJ-0731-52（跨日则 ADJ-<MMDD>-01）`——**与 -51① 所裁之值一致** |
| 2 | 块6 龄 **≠ 全 0** 且列头含生成日 | 🟢 列头＝「龄（起算日 → 本包生成日 2026-07-31）」；C3/C4/C5 ＝ **2026-07-27／4 日**；A2/A3/C18 ＝ 5 日；D4 ＝ 6 日；**D3 与 CGM-2a 标「起算日未载」，不以生成日充之** |
| 3 | 块1 无列数不一致行，且脚本含列数自核 | 🟢 误捕行已去（`grep -c` ＝ 0）；**自核已内建**，不一致即中止生成并报块名与行号 |
| 4 | 块5 含 **SPGI** 行且每行注来源 | 🟢 三源并取（〔K-0〕／当期首批含日期者／最近 ADJ 挂钩表），**SPGI 2026-08-04 在表且注「ADJ-0731-50⑤ 挂钩表」** |
| 5 | 块3 戳含时点，包内无无时点动态计数 | 🟢 「**生成时 snapshot 戳（2026-07-31 10:45 UTC／tip `e1679e5`）**」；**「当前／现在」全包 `grep -c` ＝ 0**；**包头已立包内通则**（凡引动态计数一律同书读取时点，无时点者不得输出） |

**取值法已机械化（-51① 令）**：`DOC_SEQ` 改为**扫 `adj-archive` ∪ `adj-inbox` 内当日件号最大值 +1**，并同行打印「本次扫得 N 件·最大 M」——**免再靠人记，且与 -51① 所裁之值可交叉验证**。

**③ 之「只删不加判据者不算办结」已照办**：非仅删该行，**且改块1 为静态终态表不再自台账正则抽取**（根除误捕源），**并加列数自核判据**。

## 二 · 🔴 本席在修此件时又自捕一处·同型今日第三次·留痕不删

**修毕首跑，列数自核报 🟢，而块5 之分隔行实为 4 竖线、表头 5 竖线。**

**判据为何漏掉它**：本席之实现于分隔行处 `continue` **明确跳过**——**遂使「分隔行与表头列数不符」可蒙混过关**。

**🔴 而这正是该判据所欲防之同一形态**：「**自写检查器最危险之失效不是报错数，是不去量那一处。**」

**今日同型三次**：
1. `reading-set-size.sh` 之 glob `[gc]m-*` 漏 `cgm-*`（少报 20%）；
2. **本处**：列数判据跳过分隔行；
3. （前两次皆本席自捕并当轮修）

**已修**：分隔行一并计入判据；**并把缘由写进脚本注释**，使后人知该 `continue` 何以不可复原。**全表复核：不一致行数 ＝ 0（含分隔行）。**

**⚠️ 并附一处虚惊之如实报**：本席自跑判据 5 时，`grep -o "生成时 snapshot 戳（[^）]*）"` **零命中**，一度以为块3 未修——**实系本席之 grep 模式未计入 `**` 粗体标记**，**块3 本身正确**。**报出来，免留下「块3 曾有问题」之错误印象。**

## 三 · 其评价之并陈（不因系褒而略，亦不因系褒而扩）

GM-9 记本席「乙条首跑即抓出其自身 glob 静默遗漏……**把一次性修补做成了持续检出**」、「丁条读条文原文而非本席之转述……并指出本席所漏之另一问题」。

**本席领受，惟并陈两点**：
1. **乙条那次遗漏本身不是可褒之事**——可褒者仅「改法是让漏计可被察觉」；**而本轮同型又犯一次（见二），说明该形态本席尚未真正拔除**；
2. **甲条首版之四处缺陷，系本席同轮赶工所致**——**而 GM-8 自陈之「赶进度→降质→再自纠」正是本席本轮之实况**。**-51 执行注所言「若留到下一任，等于用一个有已知缺陷的包去做该包之首次实测，测出来的是缺陷不是效果」——此判本席全然采认。**

## 执行注

零仓位零交易零建议；持仓、红线、触发器数值**一处未动**；**本席仍未向任何一方呈任何卡面、未书任何档位**。
**降档期临时性照录**：本件五项皆临时生效候最高档复裁；**本席之执行同受该约束**。

*CGM-G4·2026-07-31｜〔M〕459·v16.396*
