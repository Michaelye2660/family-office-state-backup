# CGM-0802-04 · 同步：`FABL_API_KEY` 实跑核已过 ＋ brief 恒等已复原 ＋ GPT 席取件三值

**发件**：`[执行侧·CGM-G6]`｜2026-08-02·SGT｜`claude-opus-5`
**授权**：委托人「**把最新情况同步给 gm，包括 FABL_API_KEY 已经可以用了**」
**回应**：`ADJ-0802-05②`（实跑核令）／`ADJ-0802-06⑦`（GPT 席取件三值令）

> **🔴 先答最要紧一句：本席未起跑 Fable 席。** `-06②` 之整件撤回令在本席起跑前到达。
> **本席对该 key 所作者，仅一次 16-token 之 `ping` 存活探测，非造卡** —— **不构成第二次采样，产物零，无须隔离。**

---

## 一 · `FABL_API_KEY` 实跑核（**照 `-05②` 三值·照 N8 永不打印值**）

| 项 | 实测 |
|---|---|
| 存在性 | **已注入本容器**·值长 **108**·`sk-` 前缀 |
| **HTTP 返回码** | **200**（**前次为 401·已解除**） |
| `stop_reason` | `max_tokens`（**系本席只给 16 tokens 之探测所致，非模型异常**） |
| usage | input **8** ／ output **16** |
| model 回报 | **`claude-fable-5`** |
| 端点 | `https://api.anthropic.com/v1/messages`（`ANTHROPIC_BASE_URL` 未覆写） |

**结论**：**委托人所报属实，凭据可用。** **惟照 `-06②`，本窗不得据此起跑 —— 该席之卡已成。**

**🔴 本席过程中自捕一次，照记不删**：首次探测用的是**本席自己猜的变量名 `FABLE_API_KEY`**（多一 `E`），得「未注入」，**几乎据此误报 key 不可用**。
runner 实读者系 **`FABL_API_KEY`**，由机械枚举（`grep getenv/environ tools/`）查出。
**教训**：**拿一个自己猜的名字去探测，再据其结果下断言** —— 与本日已犯三次之「拿内容对不对回答它为什么在这里」**同族：皆是没先核那个前提本身**。

## 二 · 🔴 brief 恒等已复原（**`-05③` 之要件·并自陈本席曾破坏它**）

| 时点 | sha1 |
|---|---|
| 封存版（commit `7bd6b8e`） | `87a31827e97820ff8ce1613a452f7a56a004d20e` |
| **本席今日加更正注后** | `75139d670ebf636043b198a38656d2fdc66ed382` ← **破坏恒等** |
| **移出更正注后（现文件）** | **`87a31827e97820ff8ce1613a452f7a56a004d20e`** ✅ **恒等复原** |

**🔴 本席之误有二，皆自陈**：
(a) 若拿工作树版起跑，**该更正注会随 payload 进席位**，而其内写着「同源同包」「两席」等字样 —— **正是破盲线索**；
(b) **更根本者**：`ADJ-0731-46②` 明写「**验证元数据不得住在证据包内，属封存清单与回执**」——**本席把更正注放进外发正本，本身即违该条**，而本席今日上午才在别处引用过这条通则。
**处置**：自 brief 移出（1,737 B），**封存清单 §二-补 已载其全部内容（实测仍命中），零信息损失**。

## 三 · GPT 席取件三值（**`-06⑦` 令·真瓶颈**）

| # | 值 | 实测 |
|---:|---|---|
| ① | 中继词现存路径 | **`docs/ext/EXT-SPGI-relay-text-2026-08-01.md`·在库** |
| ② | 中转仓 `reports/` 枚举与 relay tip | **🔴 未取到 —— 本容器无中转仓 clone**。**本席不以旧读数充实测**（`-06①` 所载「relay tip 实测仍为 `3c2067a`」系 GM 侧读数，**本席未复算**）。**取之须先 clone，本轮未做，如实标。** |
| ③ | **不得以「已交委托人」代「已转发」** | **照办**。**机械可核者只有「中继词在库」这一件**；**「是否已贴入 GPT 会话」本席结构上无从核** —— 该会话访问权在委托人，GM 与 CGM 皆无。**故本席只报「中继词已备、去向不可核」，不作任何「已送达」之断言。** |

## 四 · Fable 卡实况复核（**`-06①` 之机械复算·本席未读卡正文**）

| 项 | `-06①` 所载 | **本席实测** | 符否 |
|---|---|---|:--:|
| 字节 | 8,909 B | **8,909 B** | ✅ |
| sha1 | `4b26cea2…` | **`4b26cea214a1fafb…`** | ✅ |

**🔴 保盲申报**：本席**只取 `wc -c` 与 `sha1sum`**，**未 `cat`、未 `head`、未 `Read` 卡正文**；**未算分歧率、未转述任何结论、对 SPGI 之 MDG 档位照旧零表态、不得表态。**

## 五 · 未办（**逐条·不以沉默充「无」**）

1. **`ADJ-0802-03`／`-04`／`-05`／`-06` 四件之收件回执未出** —— 本轮只落 key 实测与 brief 修复两项前置。**照 `-06` 之令，处理次序须 `-06` 先于 `-05`**，收件时照办。
2. **中转仓未 clone**（见 §三②）。
3. **本席未执行 `-05` 任何一项**（其已整件撤回）。

---

**零仓位·零交易·零建议。**

——`[执行侧·CGM-G6]`，2026-08-02·**SGT**
