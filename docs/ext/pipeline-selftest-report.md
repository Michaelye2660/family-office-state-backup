# EXT 自动摆渡管道 · 段一干跑自测报告（呈 GM·ADJ-0719-17①(b)）

> **性质**：真金审计线 API 管道**段一建成 + 干跑自测**结果呈 GM 验收。**mock 回环·无 key·未触 OpenAI。** 段二（live 联调+首跑）候委托人「key 已配好」。
> **代码**：`tools/ext-pipeline/`（8 模块）｜**自测**：`python3 selftest.py` → **23/23 通过**｜**未动持仓/触发器/红线**。

## 一、建设内容七件（-17①(a)）落位对照
| # | 建设件 | 落位 | 状态 |
|---|---|---|---|
| 1 | 摆渡脚本·净室线=每次全新无状态调用（硬净室） | `netroom.py` `call_netroom(mode=live)` 每次新 client 无历史 | ✅ 建成（live 段二启用） |
| 2 | **出包检查器**（sha1 恒等 + 禁词正则 v2·相对化例外） | `checker.py` + `config.BANNED_PATTERNS` | ✅ 建成·自测过 |
| 3 | **四档路由** T1/T2/T3/T4 | `router.py` | ✅ 建成·自测过 |
| 4 | 全程留痕（request+response 全文归档主仓+sha1 锚定） | `archive.py`→`docs/ext/pipeline-archive/` | ✅ 建成·自测过 |
| 5 | 金丝雀自动首问 + 环境证明表自动生成 | `netroom.py` `CANARY_Q`/`_env_attestation` | ✅ 建成·自测过 |
| 6 | EXT-R 受控重放（席位记忆=diet 清单机械注入·防栏(a)代码化） | `netroom.load_extr_injection` + `pipeline(line=extr)` | ✅ 建成·自测过 |
| 7 | 三层核验制挂接（-11①·月度抽验接口） | `verify.py`（层2）+ 层1 内置 + 层3 归档在库 | ✅ 建成·自测过 |

## 二、干跑自测 23 用例（全过·mock 回环）
**[A] 出包检查器（8）**：A1 相对量（百分比/权重/倍数）放行 ✓／A2 美元绝对额 `$2,500,000` 拒发 ✓／A3 中文货币 `1500万美元` 拒发 ✓／A4 身份（email/电话）拒发 ✓／A5 托管行拒发 ✓／A6 sha1 恒等相符放行 ✓／A7 sha1 不符（出包漂移）拒发 ✓／A8 上呈 GM 说明生成且**不含命中原文**（防二次泄漏）✓。
**[B] 四档路由（5）**：B1 T1 事前必审（≥0.5%净值）✓／B2 T2 事后周审 ✓／B3 T3 预授权阶梯豁免 ✓／B4 T4 委托人「审」优先压倒一切 ✓／B5 T1 冻结执行+双呈 ✓。
**[C] 留痕链 + 拒发不落净室（5）**：C1 往返三件落盘 ✓／C2 request/response sha1 锚定（40 位）✓／C3 金丝雀首问置顶 + 环境证明表六项 ✓／**C4 禁词命中→拒发·不调净室（fail-closed）✓／C5 拒发件无 response 落盘（净室零触达）✓**。
**[D] EXT-R 受控重放（2）**：D1 diet 注入清单载入+sha1 ✓／D2 注入 sha1 入锚定（对表用）✓。
**[E] 三层核验制层2（3）**：E1 月度抽验 sha1+禁词复扫全过 ✓／E2 EXT-R 注入清单对表无未登记喂料 ✓／**E3 篡改探测（归档被改→抽验报漂移+禁词）✓**。

## 三、验收钢印自检（-17①(c)）
- ✅ **无出包检查器不上线**：checker 内置于 `pipeline.run_review` 每次往返·拒发即 `raise OutboundRejected`·净室零触达。
- ✅ **秘钥纪律**：`OPENAI_API_KEY` 只从环境读（`config.get_api_key`）·段一 mock 完全不需 key·永不硬编码/打印/入库。
- ✅ **材料边界 -12④**：相对量放行、绝对额/身份/托管行 fail-closed 拒发·经 A1-A5 实证。
- ⏸ **真金 T1 首用前须 GM 验收签字**：本报告即段一验收请签；段二 live 首跑后另呈金丝雀程序确认+成本首账。

## 四、段二待办（候委托人一句「key 已配好」）
1. 委托人配 `OPENAI_API_KEY`（指引 `pipeline-setup-guide.md`）+ `pip install openai`；
2. CGM live 联调（真实净室金丝雀首问 + 一次自检往返 sha1 锚定报验收）；
3. 成本实测首账替换 `pipeline-setup-guide.md` 估算；
4. **T1 首用前 GM 验收签字**→ T1/T4 现役（下一笔≥0.5%净值方案件或委托人任一「审」即首战）。

## 五、诚实标注
- 段一系 **mock 回环**·净室行为（真实模型金丝雀/成本/答卷质量）**未经 live 验证**·如实标注；live 定夺在段二。
- 禁词正则 v2 系机械闸·**对绝对额/身份/托管行为 fail-closed**（宁拒不放）·但正则非语义理解·罕见变体可能漏网→层2 月度复扫 + 归档可审为二道网。
- **日落钟**（-13(3)）：中转仓存废复议钟自**段二建成日**起算满月。

---
*编制=CGM·2026-07-19·ADJ-0719-17①(b)｜段一 mock 全建成+23/23 自测过·呈 GM 验收｜秘钥纪律=key 永不入库·段一无需 key｜段二 live 候委托人 key*
