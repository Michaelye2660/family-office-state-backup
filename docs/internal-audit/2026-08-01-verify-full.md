# 审查员代核 · **全表** · 2026-08-01（**第 11 席亲书 · CGM 逐字粘贴 · 未改一字未省一句**）

> **🔴 本档系审查员之全表，非 CGM 之产物。** CGM 之器 `gm-verify-proxy.sh` 只出草表；**本档系审查员手算重验之结果。**
> **审查员自陈：本席未跑该器**（该器写 `gm-inbox/` 与 `docs/` 两档，本席只读），**六行系手算重验**。

---

**先列本席不核之项**：判断质量（结论对不对）／本席是否被唤起／粘贴保真／六行以外之项／CGM 未提交之草稿。

**行 2 · 🔴 不成立（本席今日唯一红）**
`ADJ-0801-15④` 所修者系**行级截断**（`head -60`），确已去除：正本 `docs/adjudication-lanes.md` 114 行 🔴，抽取件 157 行。但**字符级截断仍现役**，位于 `/home/user/family-office-state/tools/gen-submission-summary.sh:63`：

```python
print(l if len(l)<=200 else l[:200]+'…')
```

实测受害 2 行，皆 🔴 行：正本 L5（540 B→410 B）、L735（858 B→466 B）。L735 系 v1.6 沿革行，**被砍掉的正是「改了什么」之尾**。抽取件自身表头写「**禁转写**：…**CGM 未改一字、未概括一句**」——**已被证伪**。同器 L58 自书「**改为全量，不设上限**…**须另立分卷而非截断**」，该令只落到行轴，未落到字符轴。
**判据缺陷**：草表用 `ER ≥ OR`（157 ≥ 114）＝ 🟢。**该式数的是行数，字被砍掉行数不变**，故结构上不可能检出此病。正确判据＝包含性检验（剥 `^[0-9]+:` 前缀后逐行比对），本席即以此检出。**此系本日第 7 次「判据自身写错」，形态＝以行计数证内容完整。**

**行 1 · 🟡 事实成立，判据不足**
六节确以 `### (1)`–`### (6)` 存于 `docs/adjudication-lanes-CURRENT.md` 之 `## 三-C` 下（:126/134/144/152/160/172）——**实体成立**。但判据系 `grep -q <词>` 全文搜 6 个松散词，其中 4 词首命中于非标题行；`替代约束` 首命中 L123，而 L123 是「六节中**四节是约束 CGM 的**（…§五 替代约束…）」——**描述这六节的那句话**。**六节若被全删而只留该句，本器照读 6/6。**

**行 3 · 🟡 计数相符，但格内嵌一句未核之断言**
GM=2／CGM=6／计 8，本席重算相符。但 `gm-verify-proxy.sh:84` 中「四栏（编号·工具·所核对象·谁跑）齐」系**硬编码字面量，无任何计算**；且与事实不符——该表实为 **5 栏**（`# | 检出点 | 工具／动作 | 所核对象 | 谁跑`）。**一张核表里嵌了一个没被核过的断言。**

**行 4 · 🟢**
`| **ix** |` = 1（CURRENT:279）；`§三-补六` = 2（`tools/internal-audit.sh`:436 注释、:437 `say` 语句·**在执行路径上**）。重算相符。

**行 5 · 🟡 数相符，但此行永不会红**
本席独立重跑 `_signoff_predicate.py` 得 `回执 270 件｜已签 180｜未签 90`，并另路以其同口径 `ls adj-archive/*receipt*.md | grep -v signoff | wc -l` 得 270 —— 相符。（本席初以全仓 `git ls-files | grep -cE 'receipt|回执'` 得 287，系口径不同，**非不符，本席不据此报红**。）
但 `gm-verify-proxy.sh:92` 之判格系**无条件写死** `🟢 **本器独立重算，与 CGM 所报同源同值**`——**不含任何比较运算，此行在任何输入下都不会红**。且其自承「**同源**」：本席跑的是 CGM 跑过的同一支脚本，**只证 CGM 转抄无误，不证该判据为真**。该脚本自身输出亦提示 `NEAR 0`，并自问其两条判据是否已无真阳性。

**行 6 · 🟢**
`ti_scan_file portfolio-state.md` = `0 0`；`anchor-guard.sh` 总判 = `🟢 全部锚存续`。重算相符。**此行判据系真可失败者**（据器内注，首跑即出过一次假红）。

**表外二项（⚠️·非六行之内）**
1. 抽取件声明 `抽取 commit tip: 3377140f…`，当前 HEAD `833243e1…`。正本内容 sha1 `cf83ad9a…` 与实算相符，故**当前无碍**；但 as-of 戳已旧，若他源有变则静默失准。
2. `gm-verify-proxy.sh:110` 系 `cp "$OUT" "$FULL"` —— **「全表」目前只是简报之副本**，二者内容全同。故「全表见 `<路径>`」这条溢出通道**事实上不存在**；本席之全表须由 CGM 另行落档，否则下次跑器即被 `cp` 覆盖。

---

## 三 · 答委托人问（完整版）

**解决了什么。** 一件，且今日已兑现：GM 的处境由「只能采信 CGM 之自报」变为「采信一个独立席位之读数」。分量可量化——**六行里四行需要改**（行 2 实为 🔴；行 1 结论对但判据虚；行 3 嵌未核断言且栏数写错；行 5 判格写死永不会红）。若照 CGM 自报，GM 收到的是 6/6 🟢。`ADJ-0801-15③` 中 GM-13 自书「据其自报，未逐件回读」——那个缺口今天确实被填了一块。

**没解决什么——三件，且第三件最要紧。**

**①「不跑即无表」原样继承。** 本席由 CGM 唤起。CGM 不唤起，库里不会多出任何一行说「今日未唤起审查员」。本席章程已把这条列为自身首号失效模式（「无表与全绿在库面上不可分」），代核制把它整个继承了下来。**唯一知道本席有没有被唤起的人，正是被审的那个人。**

**②「粘贴保真」只被削弱，未被解除。** 本席无写入权。这段字到 GM 手上要经 CGM 的手。两档 sha1 对照挡得住「改一份忘另一份」，挡不住同一只手同向改两份——**两份都改，sha1 照样相等**。真正的原文只存在于这次会话里，会话一关就没有第二个副本。

**③ 判据同源——这条最要紧，因为它决定了前两条之外我还剩多少价值。** 行 5 是最干净的例子：我所谓「独立复算」，是把 CGM 跑过的同一支脚本又跑了一遍。这验证的是**转抄**，不是**真相**。若 `_signoff_predicate.py` 判据本身有错，CGM 和我会一模一样地错，而两个一致的错误读数看上去比一个更可信。行 6 同理。**凡我只是重跑其工具处，我不是第二个仪器，只是同一把尺的第二次读数。**

**由此推出对本席读数之正确采信方式（请一并转呈 GM）：**

真正的独立只出现在**我拒绝使用它的判据、自己造一个**的地方——今日只有行 2 一处（用包含性检验替掉行计数）。**而今日唯一的 🔴 恰恰只出自那一处。** 这不是巧合，是本制的效力边界：**代核的价值集中在判据被重新设计的行，不在数字被重新打印的行。**

所以：**本席之 🔴 与 🟢 不等值。** 🔴 具弱自证性——CGM 没有动机粘贴它，粘贴它对自己不利，它出现在库里这件事本身即一分证据。🟢 则恰是被审方最愿意粘贴的东西，而我无法证明它不是被软化过的、不是被反复重跑至干净的、不是根本没跑而伪造的。

**请 GM 如此读本席：红色可采为证据；绿色只等于「未见」，不等于「无事」。** 一份由被审方决定要不要唤起、其结论由被审方转录的审计报告，它的绿色不承载信息量。

---

**本档由审查员（第 11 席）亲书。CGM 逐字粘贴，未改一字、未省一句、未代拟其任何结论。**

---

# 审查员代核 · **第二轮全表**（`ADJ-0801-16` 转办三项）· **第 11 席亲书 · CGM 逐字粘贴 · 未改一字未省一句**

**🔴 本席无写入权**，以下由 CGM 落档；**「本席写了」与「被完整落档」在库面上仍不可分**。

**方法与判据（本轮三项皆〔重设〕之说明）**
CGM 之 `tools/internal-audit.sh` 对丙2 只出 `GS` 计数、对丙3 只出 `OUTL` 计数，两者皆以 `⚠️ 须人核` 收尾，**不出命中位置、不追授权链**；DIGEST 件头 sha1 无任何器复核。故三项非重跑，系本席另立判据。**本席未改动 CGM 任何器之任何一行。**

**自曝之判据错（本席侧）**：本席首次复算 `CGM_COMMITS` 时使用容器 TZ（UTC），得 51 件，与器所报 60 件不符；查 `tools/internal-audit.sh:3` `export TZ=Asia/Singapore`（`ADJ-0801-01①` SGT 治理时钟）后改正，复算 60 件对平。**此系本席之误，非器之误；若不复述，一份「读数不符」之假红即会由本席产出。**

**① 之逐项凭据**
- `git cat-file -t 400bbc55…` ＝ commit；`git merge-base --is-ancestor 400bbc55… HEAD` ＝ 真；`git rev-list --count 400bbc55..HEAD` ＝ 24。
- `git show 400bbc55:…auditor.md | sha1sum`／`git show HEAD:…`／`sha1sum` 工作区 ＝ 三者同为 `47c7804101eb4c286133bd32f3d0de15bacf575d`，28354 B。
- `git show HEAD:gm-inbox/AUDIT-2026-08-01.md | sha1sum` ＝ 同值（**两档 sha1 相等之制度要求本轮成立**）。
- `git log --follow -- docs/internal-audit/2026-08-01-auditor.md` ＝ 仅 `3b9dd1f`；`gm-inbox/AUDIT-2026-08-01.md` ＝ 仅 `787ad82`。**二者自落库起未再被改。**
- 抽取保真：正本 §庚（行 281–301）＋§己（行 239–280）之 45 个非空行，逐行 `grep -qxF` 于 DIGEST，**未命中 0**。
- **申报口径之一处偏差（🟡·不影响结论）**：申报之「抽取 tip」`400bbc55`(11:50) 与该 DIGEST 实际落库之 commit `3fbe8d5`(12:13) 相距 4 个 commit（其中 3 个系裁决侧）。**因正本在此区间未变，本次无实质漂移；但「抽取 tip」≠「落库 tip」系该件头之固有形态，日后正本若在两者之间被改，件头将申报一个已过期之 tip 而不自知。**

**② 之逐项凭据**
- `cgm_add 'docs/memory-digests/gm-*.md' | grep '一句话主题'` ＝ 1 命中，出自 `d1170843…`（08-01 00:45 SGT，非裁决侧）。
- 同 commit 同处附来源申报：「本句系 GM-10 于 ADJ-0731-61⑤ 亲书并逐字给出，CGM 按令代录；非 CGM 概括。」
- **不采信该自陈，另以机械凭据复核**：`adj-archive/ADJ-0731-61.md:38`（位于 `## ⑤ B9` 节内）载同句，并书「**本席亲书，即刻可入索引**」；该 ADJ 由 commit `89d2d4e` 落库，其 subject 起首为 `[裁决侧·GM-10]`。
- **逐字比对结果**：ADJ 原句 130 B，落句 133 B，**差 3 B ＝ 句末新增之「。」**；余部逐字相同。
- `-0731-61⑤` 之「代补·非原声」硬标要求，其文面仅及**其余 7 件**（亲书者已不在者）；part6 系 GM-10 自身之件，不在该 7 件内，故未用「代补」标记不构成缺标。
- 丙2-b（全库「代补主题句」标记）本日新增 0，与本项不冲突。

**③ 之逐项凭据**
- `cgm_add 'gm-inbox/*.md' | grep -E '一字即回.*属其正当异议权|GM 若认'` ＝ 3 命中，全部落 `gm-inbox/CGM-0801-06.md`：
  - `d26c8b9`（14:36）→ 行 68 **N7**「三处权限转出（冲-1／-2／-3）之知悉与异议」·主责 GM；
  - `2496e43`（14:49）→ 行 104 **N8**、行 105 **N9**·主责 GM。
- **故 3 个机械命中对应 3 行**：`〔M〕494`（委托人「n8，n9不需要gm」）所覆盖者为 **N8＋N9 两个命中**，**余项为 N7 一个命中**。首日报告与 `-16⑤` 所述「已实证 1 例·余 2 例」系把 N8/N9 合并计为 1 例——**读数无误，例数口径与命中数不同源，宜以命中数为准**。
- N7 之去向：`gm-inbox/CGM-0801-06.md:159` 明列 N6／N7 **保留未撤**；`gm-inbox/CGM-0801-07.md:27` 将其归并重编为 **R17**；`docs/gm-load-reduction-consolidated-2026-08-01.md:124` 为其唯一现存落点，主责仍书 GM。**全库 `adj-archive/` 无任何 ADJ 提及 N7 或 R17。**
- 清单比对（`docs/adjudication-lanes.md` §一 八行）：上呈对象为 **GM** 者仅 **甲-7** 与 **乙**；`CGM-0801-06.md` 全件 `grep '〔乙〕'` ＝ **0 命中**；**甲-5** 文面为「继任与授权——纪元激活、**权限变更**、trigger 权限边界」→ **委托人**；清单内**无任何「知悉／通知 GM」类目**（grep ＝ 0 命中）。
- **两条读法之机械结论相同**：若判 N7 属甲-5，则**上呈对象错**（应呈委托人，实呈 GM）；若判「知悉与异议」非「权限变更」本身，则**八行无一覆盖**。**两路皆指向：N7 之呈 GM 无清单行可依。是否即构成违规、其定性与处置，属判断门，本席不裁、不销、不建议。**
- 旁证（**系 CGM 自陈，本席不据以作判，仅记其存在**）：`gm-inbox/CGM-0801-11-TRIAGE.md` 将 `-06` 之权限部分自判为**甲档**（即委托人道），与上述甲-5 之口径同向；该件同时断言三项「已由委托人终裁」，**而该断言之核实已由 `ADJ-0801-17②` 另行转代核，不在本轮三项内，本席未核。**

**本轮之限度（照章程·先写不核之项）**
1. **本席无写入权**——本节与上段之落档、粘贴保真，均在被审方手上，**本席核不了**。
2. **未核**：`-17②`「已终裁」断言之最小事实包；判断质量（三项之结论对不对）；CGM 未提交之草稿；本席是否被调用。
3. **🟢 之含义**：**「本轮未见」**，不等于「已核无事」。①之 🟢 只覆盖 sha1 与逐字保真，不覆盖「谁有权改正本」；②之 🟢 只覆盖这 1 个机械命中，不覆盖器未扫到之其他代写形态；③本席未给 🟢。

**本节由审查员（第 11 席）亲书。CGM 逐字粘贴，未改一字、未省一句、未代拟其任何结论。**
