# SWEEP-01 · 首批名单呈核 + API 三项实测报（ADJ-0730-11③⑦／-12⑤／-13·2026-07-30）

## 一、API 三项实测（-11③ 令 CGM 实测·如实报·**结论：凭据缺·须委托人配置**）

| 项 | 实测方法 | 结果 |
|---|---|---|
| (i) API 凭据与计费可用（`claude-fable-5` 可调） | 环境变量存在性核（`ANTHROPIC_API_KEY`／`ANTHROPIC_AUTH_TOKEN`／`CLAUDE_API_KEY`·**不打印值**·照 N8 秘密禁令） | **🔴 三者皆未设置** |
| (ii) 执行环境对 `api.anthropic.com` 网络可达 | `curl https://api.anthropic.com/v1/models` | **🟢 可达**——`HTTP 401`·耗时 0.179s（401＝服务端已应答、DNS/TLS/路由全通，**仅缺凭据**；非网络阻断） |
| (iii) 单卡往返实测耗量与费用 | 需 (i) 方可执行 | **⏸ 未能执行**（依赖 (i)） |

**判**：(i) 不通 → 触发 -11③ 降级预案。**但 -13 已撤销复核席、改 API-Fable 为造卡席之一**，故原降级预案（「复核席改由 CGM 于 Fable 恢复后承担」）与现架构不对应，**降级路径须重定·CGM 不自断**，呈 GM／委托人裁。三条候选（CGM 只列不荐）：

- **甲·委托人配置 API 凭据后启双卡**：在执行环境设 `ANTHROPIC_API_KEY`（**秘密值只入环境变量·永不入对话永不入仓库**·N8 硬约束）。网络既通，配好即可跑；费用照 -12④／-13⑥ 属四舍五入级。
- **乙·暂行单卡起跑、第二卡后补**：GPT 造卡腿＋CGM 机械格式核＋GM 实质核先跑首批，凭据到位后对**同批标的**补第二卡（同源同包·补卡时须确保补卡席不见第一卡——照 -13①互不见）。**代价**：-13④「两卡齐备后同一 commit 落库·GM 读前不得只见其一」在补卡批次无法满足，**该批独立性降级须入卷**。
- **丙·双卡皆走 GPT 异实例**：两席同族（皆 GPT）异会话——**CGM 提示此选项独立性最弱**（同族同期模型，分歧率天然偏低，将直接压低 -13⑤ 之分歧率端点，可能把「双卡制退役」判成假阳性），列出仅为完整性。

**费用闸登记（-12⑤／-13⑥）**：CGM 已登记 API 支出上限 **全案 $50**，触顶即停并呈报、不自行续支；每批回执附实测 tok 与费用。**当前实际支出 $0**（未调用）。

## 二、首批标的名单（-11⑦ 令 CGM 按台账拟·呈 GM 核）

**拟制原则**：①先跑证据基础最厚者（已有深查案卷／近期财报窗），使首批同时成为模板压力测试；②混入一个 U 档标的（检验 U 三件在通则下的落地）；③避开日历钩逼近之标的（8-04 IDXX／8-05 NVO／8-06 BDX 各有独立窗口，不与总验抢工时）；④3–4 标的一批（照 -11①）。

| # | 标的 | 入选理由 | 证据基础 | 注 |
|---|---|---|---|---|
| 1 | **MSFT** | 口袋进攻仓核心；刚在三轮压测中被反复评审（材料 M′系其重构造），**证据包现成且已知无盲性** | 压测卷 M 系列＋既有台账 | 压测材料**不得直接复用**（含刻意劣化版本）——须由 CGM 重出干净证据包 |
| 2 | **TSLA** | 口袋最大浮盈仓（+69.82%）、小卫星组合主体；**从未过門A** | 台账〔L-1〕＋公开披露 | 证据缺口大，可能落 U——正好检验 U 三件 |
| 3 | **SMH＋SOXQ**（半导体 ETF·合并一卡） | 口袋进攻仓最大单一敞口族；**ETF 系「组合」非「企业」，护城河口径本身须裁** | 台账〔L-1〕 | **CGM 提请 GM 先裁一个口径问题**：ETF 是否适用 MDG？若不适用，本卡改出「成分集中度＋结构风险」说明卡，不给 MDG 档 |
| 4 | **GOOGL** | 现 U 档、影子带在画；-10⑤ 明载「重评＝另案·批内随序不特案不插队」 | 門A 深查卷（ROIIC v1.1 首战）＋門B 影子带 | **按 -11 执行注：随批不插队**；本席拟排首批第 4 位——若 GM 认为仍嫌靠前，可后移 |

**排除说明（如实列）**：IDXX／NVO／BDX 因 8 月上旬各自日历窗排后批；DHR／MDT／TMO 属对冲腿医疗族，排第二批；腾讯／META 排第三批（腾讯系压测 T′材料标的，同 MSFT 之复用禁令适用）。

## 三、CGM 机械腿即启项（-11⑦·不候 API·已启）

1. 全口袋清单（进攻＋对冲＋影子带＋推定轨在册）——依台账〔A〕〔F〕〔L-1〕〔L-1b〕汇整；**〔A〕/〔F〕口径混杂三问未决**（〔M〕既载：GOOG≠GOOGL／万元口径不一／SPCX 归属），**清单出时逐项标注口径状态，不擅自调和**；
2. 现值法核实（照 P4·数据须标日期与出处·先经 data-verifier）；
3. 红线过帐（科技带口径乙／中港／口袋顶·**只登不动作**）；
4. 既有案卷摘要成卡骨架（去结论去评级·防锚定·照 -11⑤）。

## 四、候裁清单（呈 GM）

1. **API 降级路径**：甲／乙／丙择一（CGM 不荐·丙项风险已如实标）；
2. **首批名单**：采认或调整；
3. **ETF 口径**：SMH／SOXQ 是否适用 MDG——若否，替代产出形态请示下；
4. **压测材料复用禁令**：CGM 已自设「MSFT／腾讯之证据包须重出、不得复用压测材料」，请核；
5. **造卡模板封存件**：`docs/moat/sweep-01/card-builder-prompt-SEALED.md` 已落库，请核（两席共用正本·改动须新 ADJ）。

——[执行侧·CGM-G3]（claude-opus-5·临时档位），2026-07-30

---

## 附 · 新会话复测（2026-07-31·委托人问「api链条是否已通」·〔M〕411）

〔M〕395 预注册之验证路径＝「**新会话首动作单卡往返实测**」（环境变量于容器启动时注入，旧会话不热加载）。**本会话系新会话，故为该预注册之执行时点。**

| 项 | 方法 | 2026-07-30 首测 | **2026-07-31 复测** |
|---|---|:--:|:--:|
| (i) 凭据 | 环境变量存在性核（**不打印值**·N8） | 🔴 三者皆未设置 | **🔴 仍皆未设置** |
| (ii) 网络可达 | `GET /v1/models` | 🟢 HTTP 401·0.179s | **🟢 HTTP 401·0.101s** |
| (iii) 单卡往返 | **`POST /v1/messages`·model=`claude-fable-5`·max_tokens=16** | ⏸ 依赖(i) 未测 | **🔴 HTTP 401·0.086s·`authentication_error: x-api-key header is required`** |

**结论：API 链条未通，且失败点已精确定位于认证层。**
(iii) 之实测较首测更进一步——**DNS／TLS／路由／端点／模型名全部走通并得到服务端结构化应答**（`request_id` 已返回），**唯一缺件即凭据**。**非网络问题、非模型名问题、非端点问题。**

**旁核（如实）**：`ANTHROPIC_BASE_URL` **已设置**（值长 25 字符·恰为标准端点长度·**非自定义代理**·非凭据）；`ANTHROPIC_AUTH_TOKEN`／`CLAUDE_API_KEY`／`ANTHROPIC_MODEL`／Vertex／Bedrock 相关变量**皆未设置**；`~/.anthropic`／`~/.config/anthropic`／`~/.claude/.credentials.json` **皆不存在**。**即：本环境无任何形态之 Anthropic 凭据。**

**委托人侧动作（一次列示·不催）**：于 claude.ai/code → Settings → Environments → **personal** → 环境变量处设 `ANTHROPIC_API_KEY`；**秘密值只入环境变量，永不入对话、永不入仓库**（N8 硬约束，CGM 只核存在性、不读不打印）。**设后须新开会话方生效**（容器启动时注入，本会话不热加载）。

**支出闸状态**：全案上限 **$50**；**当前实际支出仍为 $0**（一次真实调用皆未发生·401 不计费）。

**对 SWEEP-01 之影响界定**：双卡制之 API-Fable 造卡席**仍未能起跑**；**惟 -14⑦ 已裁「凭据延迟不自动降为单卡」**（独立性由「同源同包＋互不见＋封存在先」保障，非由同时性保障），故**证据包可先封存、GPT 卡可先造而 CGM 扣住不呈**——**此路径不因本次复测未通而改变，造卡不延**。
