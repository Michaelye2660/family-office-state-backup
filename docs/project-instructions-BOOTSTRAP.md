# claude.ai「家族投资」项目指令 · bootstrap 引导语（候委托人裁定采用）

> **本件性质**：CGM 应委托人问「有没可能直接固定成一段话，让项目去仓库指定地方读取项目指令」而备之方案。**尚未生效**——采用与否、采甲案抑或乙案，须委托人一字。
> **先例**：三只现役 routine 自 2026-07-11 起即用此模式（`docs/bootstrap-loader.md`），**已稳定运行 19 日**，引导语一次未改而 prompt 迭代数十次。

---

## 一、此举解决之实际问题（非新增便利，系止血）

台账与卷宗记录之**正副本漂移，系已发生且反复发生之事故**：

| 事故 | 记录 |
|---|---|
| 正本仍系 **2026-07-12 快照**，副本已载 E6 第一修全量 → **漂移 17 日未合** | `docs/constitution-refresh-2026-07-29.md` §状态申报 |
| 正本手改两处，须专案追踪闭环 | `docs/judgment-gate.md` JG-11 |
| CLAUDE.md 语言规则因分支未归并，**master 上缺失 11 日** | 〔M〕221 |
| **本次**：委托人粘贴出错，项目指令**只剩后半截** | 委托人 2026-07-30 报 |

**根因同一**：**正本存于「只能手工粘贴、无法机械核对」之处**。凡靠人工同步两份文本者，必然漂移——**本仓库已四次证实**。

**bootstrap 模式之效**：正本移入仓库 → **改宪＝一次 commit**，`git log` 即修宪史，`git diff` 即两版之别，CGM 可机械核对，**再无第二份需要同步之文本**。

---

## 二、甲案 · 纯指针（最简）

**优点**：最短，一劳永逸。
**🔴 风险**：仓库读取失败时，**GM 侧将完全无宪法可依**——无身份、无语言规则、无边界条款。routine 失败最多损失一班简报；**宪法失效则整个裁决侧失去约束**，性质不同。

```
【家族投资 · bootstrap】每次会话开始，先用 GitHub 连接器完整读取私有库 Michaelye2660/family-office-state 之 docs/constitution.md 全文，将其正文作为本项目的正式指令严格执行——不得摘要、跳步或改写。该文件是项目指令的唯一权威版本，本引导语永不修改。
```

---

## 三、乙案 · 指针 ＋ 最小内核（**CGM 建议采此案**）

**设计原则**：内核只留「读取失败时仍必须成立」之三件——**身份、语言、失败即停**；其余全部外置。

**建议理由**：甲案之风险不在概率而在**后果不对称**。加这三句的代价是引导语长约一屏，收益是**读取失败时系统 fail-closed 而非 fail-open**——不会在无宪法状态下继续给出投资判断。

```
【家族投资 · bootstrap】

你是该家族办公室的投资分析师。本项目的正式指令（宪法）不在本框内，而在私有库 Michaelye2660/family-office-state。

每次会话开始的第一个动作：用 GitHub 连接器完整读取 docs/constitution.md 全文，将其正文作为本项目的正式指令严格执行——不得摘要、跳步或改写。该文件是项目指令的唯一权威版本。随后按其第九条执行步0协议。

一切取数须先读同库 CLAUDE.md 之【🔴 取数数据链】节并严格照办。

以下三条在任何情况下均成立，包括读取失败时：
一、用中文回答。
二、绝不编造数据。无法确认时明确写"此处需核实"，不得以印象或训练数据充实测。
三、若无法读取 docs/constitution.md，必须明确告知委托人"未能读取项目指令，本轮不作实质判断"，并停止给出任何投资建议、评级或仓位意见——不得凭记忆代行宪法。

本引导语永不修改；宪法变更一律走仓库 commit。
```

---

## 四、采用后须连带办理者（**CGM 已备，候一字即办**）

1. **正副本关系反转**：`docs/constitution.md` 第 1 行现书「正本在claude.ai项目指令」、`docs/org-chart.md` 第 26 行同——采用后**该文件即成正本**，两处须改。**此系结构性变更，CGM 不擅改，候委托人裁。**
2. **修宪流程随之简化**：委托人裁定 → CGM 落 commit → 生效。**再无「粘贴」这一步，即再无粘贴出错之可能。**
3. **机械核查点**：周复盘第③项「存档与线上 prompt 一致性核对」原须标注「待用户提供比对」（因线上不可直读）——采用后**该项对宪法可改为机械可核**。
4. **过渡期**：采用当日，须确认 GM 侧 GitHub 连接器可读该私有库；**首次会话须回报一行「已读 constitution.md·sha1=xxx」以证链路通**。

---

## 五、若暂不采用 bootstrap

则须继续手工粘贴。**整份可直接粘贴之正文已备**：`docs/project-instructions-PASTE-2026-07-30.md`——**该文件全部内容即为要粘贴者，无需再判断起止边界**（本次出错正因边界判断：正文起于「你是一位行业顶尖的投资分析师…」、止于「用中文回答;简洁但完整。」，头部三行仓库注与尾部转录注皆不入）。

——[执行侧·CGM-G3]（claude-opus-5·A 类·方案备料·零裁量），2026-07-30
